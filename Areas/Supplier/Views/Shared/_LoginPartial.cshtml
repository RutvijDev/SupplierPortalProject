<ul class="navbar-nav">
    @if (User == null || User.Identity == null ||User.Identity.IsAuthenticated)
    {
        <li class="nav-item">
            <a class="nav-link" asp-area="Supplier" asp-controller="Supplier" asp-action="UserProfile">Hello @User?.Identity?.Name</a>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Home" asp-controller="Home" asp-action="Logout" method="post">
                <button type="submit" class="nav-link btn btn-link border-0">Logout</button>
            </form>
        </li>
    }
</ul>